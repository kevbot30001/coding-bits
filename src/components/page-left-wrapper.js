import styled from "styled-components";

const PageLeftWrapper = styled.div`
    margin: 0;
    padding: 0;
    min-width: calc(298px + 2rem);
    width: calc((100% - 1448px) / 2 + 298px + 2rem);
    padding-left: calc((100% - 1448px) / 2 + 2rem);
`;

export default PageLeftWrapper;